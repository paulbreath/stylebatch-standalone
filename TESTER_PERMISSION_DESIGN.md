# 测试人员权限管理方案

## 🎯 需求分析

**目标**：为外部测试人员提供灵活的免费额度，方便进行产品测试。

**核心功能**：
1. 管理员可以手动设置测试人员的免费额度
2. 测试额度独立于会员系统，不受每日重置限制
3. 可以随时调整测试额度（增加/减少/清零）
4. 记录操作日志，追踪额度变更

---

## 📊 方案设计

### 1. 用户类型扩展

在现有的用户类型基础上，添加"测试人员"标识：

| 用户类型 | membershipType | 特点 |
|---------|---------------|------|
| 免费用户 | free | 每日 5 张，每天重置 |
| VIP 会员 | monthly/quarterly/yearly | 按套餐额度，到期后降级 |
| 企业版 | enterprise | 无限额度 |
| **测试人员** | **tester** | **管理员手动设置额度，不自动重置** |

### 2. 数据库设计

**方案 A：复用现有 `user_quotas` 表**（推荐）

添加字段：
- `isTester`: boolean - 是否为测试人员
- `testerQuota`: number - 测试人员专属额度
- `testerNote`: string - 测试人员备注（如：外部测试、内部测试）

**优点**：
- 不需要新建表
- 逻辑集中，易于维护
- 可以灵活切换测试/正式状态

### 3. 额度逻辑

**测试人员额度规则**：
1. `isTester = true` 时，使用 `testerQuota` 而不是 `totalQuota`
2. 测试额度**不会每日重置**
3. 管理员可以随时调整 `testerQuota`
4. 测试额度用完后，提示"测试额度已用完，请联系管理员"

**优先级**：
```
企业版（无限） > 测试人员（手动额度） > VIP 会员（套餐额度） > 免费用户（每日 5 张）
```

---

## 🔧 实现细节

### 1. 后端接口

#### 1.1 设置测试人员额度
```typescript
// POST /api/trpc/admin.setTesterQuota
{
  userId: number,
  quota: number,
  note: string
}
```

#### 1.2 查询所有测试人员
```typescript
// GET /api/trpc/admin.listTesters
返回：[{
  userId: number,
  name: string,
  email: string,
  testerQuota: number,
  remainingQuota: number,
  testerNote: string,
  createdAt: Date
}]
```

#### 1.3 移除测试人员身份
```typescript
// POST /api/trpc/admin.removeTester
{
  userId: number
}
```

### 2. 前端页面

#### 2.1 管理后台路由
- `/admin` - 管理后台首页
- `/admin/testers` - 测试人员管理

#### 2.2 功能模块
1. **用户搜索**：
   - 支持按邮箱、用户名搜索
   - 显示用户基本信息和当前额度

2. **设置测试额度**：
   - 输入额度数量（如：100 张）
   - 添加备注（如："外部测试 - 2024Q1"）
   - 确认后立即生效

3. **测试人员列表**：
   - 显示所有测试人员
   - 显示剩余额度和使用情况
   - 支持调整额度或移除身份

4. **操作日志**：
   - 记录谁在什么时间设置了谁的额度
   - 记录额度变更历史

---

## 🛡️ 权限控制

**管理员验证**：
- 只有 `role = 'admin'` 的用户可以访问管理后台
- 使用 `adminProcedure` 保护所有管理接口
- 前端路由也需要检查管理员权限

**安全措施**：
- 所有操作记录日志
- 额度设置有上限（如：最多 10000 张）
- 敏感操作需要二次确认

---

## 📋 实施步骤

1. ✅ 扩展 `user_quotas` 表（添加 `isTester`, `testerQuota`, `testerNote`）
2. ✅ 修改额度检查逻辑（支持测试人员）
3. ✅ 实现后端管理接口
4. ✅ 创建管理后台页面
5. ✅ 测试完整流程
6. ✅ 保存检查点

---

## 💡 使用场景

### 场景 1：外部测试人员
```
管理员操作：
1. 搜索测试人员邮箱
2. 设置额度：100 张
3. 备注："外部测试 - 产品验证"

测试人员体验：
- 看到"测试额度：100 张"
- 可以正常使用转换功能
- 额度用完后提示联系管理员
```

### 场景 2：内部测试
```
管理员操作：
1. 批量设置内部测试账号
2. 每人 500 张额度
3. 备注："内部测试 - 性能压测"

测试团队：
- 不受每日限制
- 可以充分测试产品
- 管理员可随时查看使用情况
```

---

## 🎯 预期效果

1. **灵活性**：管理员可以根据需要随时调整测试额度
2. **可控性**：所有操作有日志，额度使用可追踪
3. **用户体验**：测试人员不受每日限制，可以充分测试
4. **安全性**：只有管理员可以操作，防止滥用
